/**
 * PLACEHOLDER CONSTANTS — NOT FUNCTIONAL
 *
 * All URLs and IDs in this file are non-functional placeholders.
 * Replace each value with the real production value before going live.
 * Changing a value here updates every CTA on the page automatically.
 */

/**
 * LINE Official Account friend-add URL.
 * PLACEHOLDER — replace with the real LINE add-friend URL before launch.
 */
export const LINE_FRIEND_URL = 'https://line.me/R/ti/p/PLACEHOLDER_LINE_ID'

/**
 * Secondary booking URL (e.g. Calendly / STORES予約).
 * PLACEHOLDER — replace with the real booking page URL before launch.
 */
export const SECONDARY_BOOKING_URL = 'https://example.com/booking-placeholder'

/**
 * Google Tag Manager container ID.
 * PLACEHOLDER — replace with the real GTM-XXXXXXX ID before launch.
 * When unset / left as placeholder the Analytics component renders nothing.
 */
export const GTM_ID = 'GTM-PLACEHOLDER'
